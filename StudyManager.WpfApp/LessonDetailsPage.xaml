<Page x:Class="StudyManager.WpfApp.LessonDetailsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Lesson Details">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Lesson Information" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock x:Name="TopicText" FontSize="16" Margin="0,5"/>
            <TextBlock x:Name="DateText" FontSize="14" Margin="0,5"/>
            <TextBlock x:Name="TimeText" FontSize="14" Margin="0,5"/>
            <TextBlock x:Name="TypeText" FontSize="14" Margin="0,5"/>
        </StackPanel>

        <Button Grid.Row="2" Content="Back" Width="100" HorizontalAlignment="Left" Click="BackButton_Click"/>
    </Grid>
</Page>